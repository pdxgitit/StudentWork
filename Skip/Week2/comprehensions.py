# write a function (area) that takes a tuple of two values and returns the
# result of multiplying those two values together

dimensions = [
    (5, 5),
    (10, 10),
    (2.2, 2.3),
    (100, 100),
    (8, 70),
]

def area(tup):


# now write a list comprehension that applies the area function to each value
# in the dimensions list
